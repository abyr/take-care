{{> head}}

<h1>{{title}}</h1>

<div>{{> filters}}</div>

<div>{{> pagination}}</div>

<div>
    {{^errors}}
        No errors
    {{/errors}}
    {{#errors}}
        {{> error}}
    {{/errors}}
</div>

<div>{{> pagination}}</div>

{{> pagination-script}}

{{> footer}}
